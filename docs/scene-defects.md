# Opening scene defects

This discrepancy ledger supplements the 33 requirements in recovery-checklist.md.
A fixed component does not close a scene or establish retail parity. Timing,
input, audio and pixel comparisons remain independent requirements.

| Defect | Current result | Next owner or verification |
| --- | --- | --- |
| Obstructing room module | Fixed in ordinary replay; native observation agrees on 19 transforms across three modules | Complete geometry/collision sweep remains R18 |
| Gurney opaque black fill | Source falloff pass retains opacity; owned audit passes; new live frame shows the open wheel | Matched surfaces, shadows and camera phase |
| Skin texture over preview shirt | Clothing preserves its NIF texture; new live frame shows the dark shirt | Current preview material and lighting comparison |
| Missing top-left item notice | Original gift icon, bracket, bitmap font and item text appear during Classic Pack | Queue timing, fading, wrapping, glow and retail event order |
| Startup notification scheduling | Source initialization includes winning embedded declarations and all standalone quest scripts; 252 retained native initial clocks match exact bits. Shared clocks, cold consistency and main-menu lifetime pass selected checks; room-51 retains clocks through New | Exact block admission, MenuMode execution, mutable delays, dynamic quest start/stop and aligned first-message sequence remain open |
| Retail callback-assisted New stays white | Keyboard-only room-53 clears the stale StartMenu state. Harness callback rejection writes no command; paired selected-button Enter dismisses Tribal and both runs subsequently show Classic. Full gate passes | Use keyboard input for later checkpoints; exact event timing remains open |
| Tap spans a load boundary | Requested duration now reaches both local engine leases; live 25 ms check and full gate pass | Replay the movie/message boundary and compare consumed edges |
| Default message button casing | Corrected: ShowMessage now reads its own owned declaration; synthetic, owned, full-gate and live room-48 checks pass | Component closed; broader message layout and timing remain open |
| Opening movie audio underruns | Room-50 reported 556 underruns at 3.56 seconds; room-51 reported zero at 8.97 seconds before its ordinary skip | Intermittent startup/refill accounting and audible impact remain unverified; no new cinematic acceptance claim |
| Radio/remaining HUD notices | Open | Discovery events and remaining source HUD branches |
| Missing Doc response gestures | Four source clips pass finite-playback and resumption audit | Complete matched motion, speech overlap and handoff timing |
| Chair exit and walking | Owned audit passes exit, root continuity, NAVM travel and arrival | Ordinary replay, native pose/timing, later furniture entry |
| Cigarette schedule | Source IDLE repeat interval, PACK wait and ANIO attachment bind | Native clocks, interruption/resumption and cigarette start/stop |
| KF animation sound | Crossed source events now dispatch to owned SOUN/audio; selected component tests pass | Live dispatch, matched audio, random sequence and wet/reverb behavior |
| Actor audit shutdown | Fresh native actor checks pass but report six ObjectDB instances at shutdown | Identify retained resource types and release their owners |
| Cigarette smoke | Source particle owner not yet identified | Trace effect/event attachment; no replacement puff system |
| Preview blinking | Source FaceGen lifecycle and original TRI targets pass native component audit | Ordinary Reflectron replay and matched timing |
| Opening haze/distortion/focus | Source blur kernels/prefilter bind; selected tests pass | GPU replay, double vision, radial and remaining channels |
| Reflectron background blur/DOF | Separate source menu-background capture/blur is wired; declaration tests pass | Fresh GPU replay and matched focus/background pixels |
| Room hue, lighting and windows | XEMI LIGH/REGN bindings added; all 25 selected native light input triples, radii and dimmers match exact bits at a night checkpoint | Live clock phase, light selection, attenuation, shadows, fog and final pixels |
| CELL directional axis | Corrected the rotation convention; all three source-derived ray components match native Float32 bits. Synthetic/owned checks, full gate and ordinary room-60 binding pass | Component closed; direction alone does not resolve the bright room |
| Point-light reach expanded by 41 percent | Removed the unsupported multiplier from all three creation paths. Native radius/scale evidence, synthetic binding checks, selected owned audit and full gate pass. Ordinary room-61 binds all 25 source radii exactly after unit conversion; floor region error falls from 32.46 to 10.28 and ceiling from 37.25 to 13.22 | Component closed; regional diagnostics do not establish aligned pixels or light selection/shadow parity |
| Fog distance uses the wrong space | Corrected to projected vertex distance and source unit conversion. Owned programs/native input agreement, synthetic GPU projection checks and full gate pass | Component corrected; camera frustum, active fog toggles and matched GPU contribution remain open |
| Window angular opacity uses the wrong curve/stage | Corrected linear fragment opacity to the source smooth vertex curve. Ten owned variants, synthetic/owned/GPU checks and full gate pass. Checked beam error falls from 33.97 to 14.79 colour levels in room-63 | Active native no-lighting variant, fog/blend toggles and matched final pixels; ceiling region error increases |
| Later animation attachment misses room environment | Fixed through general cell insertion/transfer binding and preview isolation. GPU lifecycle checks and ordinary room-64 pass; all 675 declared instance fog inputs match the source, including the later attachment | Component closed; selected native fog toggles and per-pixel contribution remain open |
| Dynamic actor geometry loses source-model trace | Skeleton and ANIO roots retain model provenance. Ordinary room-64 resolves both previously missing source-model entries with no trace errors or lost events | Component closed; this does not certify animation or pixels |
| Reference material emittance omitted | Winning LIGH/REGN sources and shared sky/time now bind eligible material instances. Synthetic/owned/GPU lifecycle and full gate pass. Ordinary room-66 matches all 20 sampled native RGB triples and material multipliers | Component corrected; native GPU draw association, HDR mode and complete weather/pixel coverage remain open |
| No-falloff surfaces bypass the source shader | Unified the no-lighting material path, including nine window surfaces. Source zero-colour fallback and managed colour/UV channels remain in that owner. All eight original Vigor pages pass forward/backward and allocation checks | Matched non-falloff/falloff draws, source fog/blend composition and full controller timing |
| GPU material audit shutdown | Fixed: explicitly free the local RenderingDevice. Its three retained instances disappear in the normal-renderer verbose audit; assertions and full gate pass | Component closed; the separate actor-audit shutdown issue remains open |
| No-lighting surfaces use default fog composition | Source vertex fog and destination-factor selection now bind all 68 live no-lighting surfaces. All 743 declared fog instances have exact source inputs; 20 native alpha/pass records corroborate the effect inputs. Owned/GPU/full-gate checks pass; beam-region error moves from 6.83 to 6.18 | Native GPU draw/frame association, per-pass fog admission and premultiplied-program selection; wall/ceiling region errors rise slightly |
| World camera far plane differs | Native world camera reports 5000 game units; OpenNV uses 200 metres | Bind the source far-plane owner; do not equate it to fog range without proof |
| Excess room brightness before HDR | Ten retained GPU surfaces and eleven submitted pass constants now expose the image-space chain. Native target/cinematic/tint/fade bytes agree; excess brightness already exists in the incoming scene | Selected native pass/light admission, material response, fog and shadows; final pixels remain open |
| Room vertex colours ignored | Fixed: geometry colour buffers determine the runtime shader binding. Synthetic, 20-model owned audit and full gate pass; ordinary room-56 trace confirms all 175 lit instances. Seven native bound properties independently enable the colour bit. Checked wall regions improve | Component closed; remaining floor/ceiling/window brightness, light contribution and complete final pixels stay open |
| Stage global writes never reach the clock | Fixed through a general compiled-reference owner. Synthetic/owned checks, full gate and ordinary room-55 replay pass. Light RGB matches improve from 19/25 to 25/25, with all dimmers matching; the live night hour comes from the source stage | Renderer contribution remains too bright; authored ForceWeather, conditional/multiple result scripts and full stage execution remain separate gaps |
| Doc resting pose at the name prompt | Same quest/menu checkpoint shows different head direction and hand positions | Bind animation phase, dialogue release and aiming; no pose acceptance from the menu match |
| Name prompt text response | Original text and panel appear on both sides; prompt/name glyphs remain visibly thinner in OpenNV | Per-tile font selection, glow and text composition |
| Cold global/sky continuity | Script globals, Float32 clock and sky identities persist; synthetic cold checks pass | Ordinary opening/Continue and exterior weather owner |
| Reported clock blinking | Open | Continuous instance/material/controller capture |
| Original Vigor interface | All eight native controls pass forward/backward, allocation limits, point restoration and completion | Matched framing, sound/gamepad and ordinary activation |
| Psychology, tag/trait and farewell UI | Replacement panels still exist after creation | General source dialogue and original menus |
| Pip-Boy, doors and exterior | Open | Direct presentation, ordinary input and persistent travel |
| Loot, tutorial skull and pool table | Open | Complete interaction/physics/persistence sweep |

Private `opennv-hud-45` frame 5 shows the live notice and Classic Pack, frame 10
the gurney and a response pose, and frame 12 the corrected shirt in Reflectron.
These are private local outputs, not repository assets. The current component
changes passed the full gate on 2026-09-05, plus fresh owned light/audio and
native actor audits. Publication and live acceptance remain separate steps.

Keep the comparison window closed during implementation. Reopen it for bounded
matched checkpoints and retain the resulting discrepancy/evidence. Mismatched
menus are not verification. Skip the already exercised opening movie.
