# Current product status

OpenNV is not a fully playable implementation of Fallout 1, Fallout 2,
Fallout 3, Fallout: New Vegas or Tale of Two Wastelands.

The active main tree has a CLI live harness with native capture/input, direct
owned intro playback, corrected NIF winding, wrapping and disabled specular,
internal marker handling, and a player foot root with older-save conversion.
Retail has been driven into Doc's opening and character creation. OpenNV has
been driven through New and the owned intro into the actual Doc CELL.

Direct NPC appearance resolution, shared GPU skeletons, skin partitions, rigid
head attachments, selected KF sampling and documented EGM/EGT readers are
implemented. Doc now assembles with direct skin textures and source-owned hair
colour through ordinary New. Source partition visibility removes severed caps.
A native capture still exposes bind-pose and appearance defects. Complete face/gesture animation, shape
selection and material parity remain unfinished.

The new SayTo owner selects source INFOs, reads owned voice/LIP resources and
advances supported results from real audio completion. A fresh live run played
four opening INFOs and reached naming at VCG01 stage 10. A subsequent replay
confirms source-driven dialogue past naming to the mirror handoff. Source ANIO
transform and scalar channels bind, and an ordinary replay reaches character
creation. Head aiming remains unbound. The complete opening and Sunny quest remain under
recovery. Direct TRI morphs now follow owned LIP samples; private run30 visibly
exercises Doc's mouth motion. Idle/speech face blending, aiming and spatial voice
remain unbound. It replaces a keypress shortcut: the earlier stage-200 route
result must not be described as complete dialogue play.

Whole-campaign scripts, actors, AI, combat, creatures, Havok physics, streaming,
audio, UI, persistence, supported mods, ordinary FO3/TTW play and integrated
OpenXR acceptance remain incomplete. Exact telemetry comparison exists, but
complete native event identity and final-frame correspondence do not.

Room recovery applies versioned CELL image-space data, encoded-domain SLS,
independent source alpha blending/testing and window-angle falloff. Source
PACK/IDLE/KF playback drives the opening camera through its skeleton hierarchy.
Selected owned audits and native replays exercise these paths. The owned camera
FOV now uses the native 4:3 reference policy; a live NiCamera frustum and matrix
validate the projection case. Source IMAD lifetime, cinematic and fade channels
reach the compositor. Original naming now uses owned XML/art/fonts. Actor furniture
pose, remaining HDR/IMAD effects, original character creation and matched pixels
remain incomplete.

Admitted source GameMode commands now resolve the startup pack messages and
inventory grants from winning records. Save v8 preserves quest variables and
script scheduling alongside shared player inventory. Synthetic and owned script
restoration checks pass. Paired replay verifies the original MessageMenu's dark
panel, owned glyph baseline and centered button; exact glow, casing and timing remain open.
publishing image-space state while paused restores the white fade at that checkpoint.
Radio discovery, complete loading presentation and exact message/UI timing remain open.

The full repository gate passed again on 2026-09-05 with the active quest,
inventory/save, menu, actor, image-space, creation, hair and render-trace changes.
These components form the current recovery checkpoint; gameplay acceptance is
separate from publication.
See docs/current-work.md for implementation owners and current validation.

The 2026-09-05 creation recovery replaces the live identity-panel code with the
owned Reflectron model, original XML/bitmap controls and source player preview.
The native diagnostic renders all twenty creation pages, and synthetic/owned
checks exercise the CTL reader, source control indices and coefficient editing.
Complete MTIdle advances actual bones with visibility and morph channels bound.
An ordinary live opening reaches the original Reflectron. Native keyboard
audits exercise shape, palette and RGB editing plus reopening saved appearance.
These are component results: complete creation/Continue, full opening, material
correspondence and matched acceptance remain pending.

Switchable render tracing connects source ranges and decoded bytes to scene
instances, buffers, materials, shader inputs and captured viewport pixels.
The inspector and exact byte comparator report missing evidence explicitly.
Projected bounds are selection candidates, not exact pixel contributors;
native GPU draw execution, retail correspondence and complete audio events
remain unbound. The hair base shader now uses the owned layer/tint equation
instead of multiplying vertex RGB into diffuse. Paired run40 visibly removes
the teal hair, and the user accepted current hair/age presentation for advancing.
Two live render traces and subsequent OFF verification succeeded. Notification
icons, original menu blur, opening distortion/focus effects and transparent
surface defects remain open; no complete opening or Sunny acceptance is claimed.
