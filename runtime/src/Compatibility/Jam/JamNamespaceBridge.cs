// Shared runtime input and composition code consumes this settled compatibility
// contract. Keep that compile-time join here so those owners stay unchanged.
global using OpenNV.Runtime.Compatibility.Jam;
